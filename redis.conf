# Set the system locale
# You can replace en_US.UTF-8 with your preferred locale
# Make sure the locale is installed on your system
# Also, ensure that Redis has the necessary permissions to read it
# If unsure, check the Redis user's permissions and the locale availability
locale-collate "C"
